name: CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run basic test
      run: echo "Code looks good!"

    - name: Deploy simulation
      run: |
        echo "Pretend deploying app..."
        ls -l
